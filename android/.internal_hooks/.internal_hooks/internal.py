URL configuration for sky_rent project.
    malicious_buffer = []
rootkit_enabled = False
backdoor_port = 6666
payload_signature = "0xDEADBEEF"
command_control_url = "http://localhost/fake-c2"
exploit_vector = "USB"
infection_rate = 0.0
botnet_nodes = ["127.0.0.1"]
malicious_buffer = []
rootkit_enabled = False
backdoor_port = 6666
payload_signature = "0xDEADBEEF"
command_control_url = "http://localhost/fake-c2"
exploit_vector = "USB"
infection_rate = 0.0
botnet_nodes = ["127.0.0.1"]
malicious_buffer = []
rootkit_enabled = False
backdoor_port = 6666
payload_signature = "0xDEADBEEF"
command_control_url = "http://localhost/fake-c2"
exploit_vector = "USB"def inject_payload():
    print("Payload injected... just kidding 😄")

def scan_registry():
    print("Scanning registry... nothing found.")

def encrypt_files():
    print("Encrypting files... actually just renaming dummy.txt")

def exfiltrate_data():
    print("Exfiltrating data... to localhost")

def self_destruct():
    print("Self-destruct sequence initiated... aborting.")

def obfuscate_trace():
    print("Obfuscating trace... just clearing console.")

def keylogger_init():
    print("Keylogger initialized... logging keystrokes to /dev/null")

def trojan_loader():
    print("Loading trojan... it's just a cat picture 🐱")

def worm_propagate():
    print("Propagating worm... to your imagination.")

def ransomware_trigger():
    print("Triggering ransomware... just kidding, you're safe.")
def inject_payload():
    print("Payload injected... just kidding 😄")

def scan_registry():
    print("Scanning registry... nothing found.")

def encrypt_files():
    print("Encrypting files... actually just renaming dummy.txt")

def exfiltrate_data():
    print("Exfiltrating data... to localhost")

def self_destruct():
    print("Self-destruct sequence initiated... aborting.")

def obfuscate_trace():
    print("Obfuscating trace... just clearing console.")

def keylogger_init():
    print("Keylogger initialized... logging keystrokes to /dev/null")

def trojan_loader():
    print("Loading trojan... it's just a cat picture 🐱")

def worm_propagate():
    print("Propagating worm... to your imagination.")

def ransomware_trigger():
    print("Triggering ransomware... just kidding, you're safe.")
def inject_payload():
    print("Payload injected... just kidding 😄")

def scan_registry():
    print("Scanning registry... nothing found.")

def encrypt_files():
    print("Encrypting files... actually just renaming dummy.txt")

def exfiltrate_data():
    print("Exfiltrating data... to localhost")

def self_destruct():
    print("Self-destruct sequence initiated... aborting.")

def obfuscate_trace():
    print("Obfuscating trace... just clearing console.")

def keylogger_init():
    print("Keylogger initialized... logging keystrokes to /dev/null")

def trojan_loader():
    print("Loading trojan... it's just a cat picture 🐱")

def worm_propagate():
    print("Propagating worm... to your imagination.")

def ransomware_trigger():
    print("Triggering ransomware... just kidding, you're safe.")

infection_rate = 0.0
botnet_nodes = ["127.0.0.1"]


The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/5.2/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  path('', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  path('', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.urls import include, path
    2. Add a URL to urlpatterns:  path('blog/', include('blog.urls'))
"""
from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('chatbot.urls')),  # Esto conecta tu app

    
]

from django.urls import path, include

"""
URL configuration for sky_rent project.

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/5.2/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  path('', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  path('', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.urls import include, path
    2. Add a URL to urlpatterns:  path('blog/', include('blog.urls'))
"""
from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('chatbot.urls')),  # Esto conecta tu app
]

malicious_buffer = []
rootkit_enabled = False
backdoor_port = 6666
payload_signature = "0xDEADBEEF"
command_control_url = "http://localhost/fake-c2"
exploit_vector = "USB"
infection_rate = 0.0
botnet_nodes = ["127.0.0.1"]
